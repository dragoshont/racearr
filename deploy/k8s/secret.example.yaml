# Copy to secret.yaml (git-ignored) and fill in, or manage these keys with your
# own secret tooling (Sealed Secrets, External Secrets, SOPS, etc.).
apiVersion: v1
kind: Secret
metadata:
  name: racearr-secrets
type: Opaque
stringData:
  RADARR_API_KEY: "changeme"
  SONARR_API_KEY: "changeme"
  # Optional — omit if qBittorrent bypasses auth for in-cluster clients.
  QBIT_USERNAME: ""
  QBIT_PASSWORD: ""
